{
  "name": "costa_web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "serve -s dist",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "Dependencies": {
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "typescript": "~5.6.2",
    "vite": "^6.0.5"
  }
}
